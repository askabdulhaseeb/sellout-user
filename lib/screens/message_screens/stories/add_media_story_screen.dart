import 'package:flutter/material.dart';

class AddMediaStoryScreen extends StatelessWidget {
  const AddMediaStoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
